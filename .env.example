# Copy to .env.local for local dev, .env.production for prod builds
# Android emulator → 10.0.2.2 (points to host machine)
# Physical device  → your machine's LAN IP e.g. 192.168.1.42
API_BASE_URL=http://10.0.2.2:3000/api/v1
